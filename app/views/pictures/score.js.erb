<% @pictures.each_with_index do |p,index| %>
$('.top-ten-image-<%= index %>').attr('src', '<%= Picture.find(p.id).image %>');
$('.likes-<%= index %>').html('<%= Picture.find(p.id).score %> Likes');
$('a.score-<%= index %>').attr('href','/pictures/<%= p.id %>/score');
<% end %>